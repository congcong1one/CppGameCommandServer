# PROJECT_STATE — 单一事实源

> 本文件是当前进度的唯一权威记录。Agent每次会话开始先读本文件，每次有效会话结束必须更新本文件。
> 用户不需要记住自己进行到哪一步；以本文件为准。

## 当前状态（2026-08-09 更新）

- **当前模式**：学习
- **当前交付门**：A（C++准入包）
- **当前任务卡**：卡1（CMake多文件工程）
- **已通过的验收**：本地仓库 + 远程备份就绪
- **当前唯一任务**：用户在15分钟时间盒内自行搭建一个最小多文件CMake工程（见下方“下一动作”）
- **最近一次构建/测试命令与结果**：无（尚无C++工程）
- **最近环境/文档验证**：2026-08-09，GitHub HTTPS与SSH认证成功，`git pull --rebase`返回`Already up to date`；V1.2原始规范已完成19页渲染与结构化审计，原文未纳入Git

## 下一动作

用户主写最小多文件CMake工程的第一版；Agent只提供Review和验证，不代写核心答案。

完成标准：

- 至少包含一个可执行入口、一个独立头文件、一个独立实现文件和根目录`CMakeLists.txt`。
- `cmake -S . -B build`、`cmake --build build`和运行生成的可执行文件均成功。
- 用户能指出编译单元、头文件声明、链接目标分别在哪里；本任务完成后再单独验收90秒编译链接口述。

## 阶段进度看板

### Gate A：C++准入包（进行中）

- ☑ 本地仓库 + 远程备份就绪（2026-08-07，commit 123286a 推送成功）
- ☐ 15分钟建立多文件CMake工程
- ☐ 90秒讲清编译链接
- ☐ RAII文件描述符闭卷实现
- ☐ BoundedQueue支持容量、等待、关闭和join
- ☐ PacketCodec处理半包、多包、非法长度
- ☐ 内存版装备强化失败时状态不变
- ☐ TSan发现并修复一次数据竞争
- ☐ 安排校准面试（Gate A全部勾选后才进入）

### Gate B：端到端求职启动包（未开始）

### Gate C：反馈强化（未开始，默认MySQL事务）

## 升级判定规则（Agent必须主动检查）

- 仅当上方某个Gate的全部验收项勾选后，才允许宣布进入下一阶段。
- 每次会话结束时，Agent必须对照看板说明：本次推进了哪些项、还差哪些项、是否达到升级条件。
- Gate A完成 → 提醒安排校准面试，阻止"再准备一点"。
- Gate B完成 → 提醒开始主要投递，不把MySQL当前置门槛。

## 变更历史

| 日期 | 变化 | 依据 |
|---|---|---|
| 2026-08-07 | 初始化状态文件；项目处于Gate A起点，任务卡0—2 | 仓库仅有初始提交，无C++工程 |
| 2026-08-07 | 勾选"仓库+远程备份就绪"，任务卡推进至卡1（CMake） | commit 123286a 成功推送到 origin/main |
| 2026-08-07 | 新增AGENTS.md跨Agent协作协议；Skill迁入工程 .qoder/skills/；本仓库成为多主机Agent进度共享总线 | 用户明确要求跨主机Agent经本仓库同步进度 |
| 2026-08-09 | 对照V1.2原始规范优化全部非src协议层：补公开README、统一状态/Git规则、强化隐私与模板边界；未改变冻结方案或Gate验收 | 原始规范19页渲染与结构化审计；本次文档一致性、链接与隐私检查 |
