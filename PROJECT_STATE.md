# PROJECT_STATE — 单一事实源

> 本文件是当前进度的唯一权威记录。Agent每次会话开始先读本文件，每次有效会话结束必须更新本文件。
> 用户不需要记住自己进行到哪一步；以本文件为准。

## 当前状态（2026-08-10 更新）

- **当前模式**：学习
- **当前交付门**：A（C++准入包）
- **当前任务卡**：卡2（90秒编译链接口述）
- **当前任务AI自治等级**：AI-0（闭卷口述）
- **已通过的验收**：本地仓库 + 远程备份就绪；15分钟建立多文件CMake工程
- **当前唯一任务**：不看资料，在90秒内讲清`main.cpp`与`t1.cpp`如何经过预处理、编译和链接形成`CppCommandServer`，并说明漏头文件、漏声明/类型错误、漏实现文件分别在哪个阶段失败
- **最近一次构建/测试命令与结果**：从仓库根目录配置成功；`main.cpp`与`t1.cpp`分别编译并链接为`CppCommandServer`；运行输出`hello world`和`hello world 2`；clangd基于导出的编译数据库检查两个源文件均为0错误
- **最近环境/文档验证**：2026-08-10，Gate A卡1经AI-1 Review完成；根目录多文件工程的配置、构建、运行和clangd检查均通过；CMake仍有缺少`cmake_minimum_required()`的开发警告，不影响本卡验收

## 下一动作

Gate A卡1已经完成，当前进入90秒编译链接口述。用户不看资料，围绕当前工程说明从两个编译单元到一个可执行目标的全过程，并准确区分预处理、编译和链接错误。

完成标准：

- 90秒内覆盖预处理、编译、链接三个阶段。
- 能指出当前两个编译单元、共享声明和最终链接目标。
- 能判断找不到头文件、类型/语法错误、缺少函数定义各自的失败阶段。

## 阶段进度看板

### Gate A：C++准入包（进行中）

- ☑ 本地仓库 + 远程备份就绪（2026-08-07，commit 123286a 推送成功）
- ☑ 15分钟建立多文件CMake工程（2026-08-10，根目录配置、构建、运行及clangd检查通过）
- ☐ 90秒讲清编译链接
- ☐ RAII文件描述符闭卷实现
- ☐ BoundedQueue支持容量、等待、关闭和join
- ☐ PacketCodec处理半包、多包、非法长度
- ☐ 内存版装备强化失败时状态不变
- ☐ TSan发现并修复一次数据竞争
- ☐ 安排校准面试（Gate A全部勾选后才进入）

### Gate B：端到端求职启动包（未开始）

- ☐ 测试客户端完成连接、查询和强化
- ☐ Session重复关闭安全并最终析构
- ☐ SendQueue保持有序且有上限
- ☐ 命令线程与网络线程边界清晰
- ☐ 请求中断线后晚到响应安全丢弃
- ☐ ASan无已知问题
- ☐ 用户能画线程/所有权图并讲5分钟链路
- ☐ Linux下build/test/run/stop可重复，日志支持一次故障定位
- ☐ 本地检查脚本或CI至少有一个自动质量门
- ☐ 一次AI-2证据包通过：任务简报、完整Diff Review、拒绝/修正、硬验证、贡献说明
- ☐ 开始主要投递

### Gate C：反馈强化（未开始，默认MySQL事务 + 最小Docker/Compose + AI-3证据）

- ☐ request_id唯一约束生效
- ☐ 同一请求重复提交只产生一次结果
- ☐ 不同请求并发改同一装备保持正确
- ☐ 金币不足不会变负
- ☐ 任意中间失败全部回滚
- ☐ 装备变化流水可用于审计
- ☐ 数据库错误不会令进程崩溃
- ☐ Docker/Compose可以启动、停止并完成一次恢复演练
- ☐ 一次AI-3端到端证据通过，用户能说明风险、验证、发布和回滚

## 升级判定规则（Agent必须主动检查）

- 仅当上方某个Gate的全部验收项勾选后，才允许宣布进入下一阶段。
- 每次会话结束时，Agent必须对照看板说明：本次推进了哪些项、还差哪些项、是否达到升级条件。
- Gate A完成 → 提醒安排校准面试，阻止"再准备一点"。
- Gate B完成 → 提醒开始主要投递，不把MySQL当前置门槛。

## 变更历史

| 日期 | 变化 | 依据 |
|---|---|---|
| 2026-08-07 | 初始化状态文件；项目处于Gate A起点，任务卡0—2 | 仓库仅有初始提交，无C++工程 |
| 2026-08-07 | 勾选"仓库+远程备份就绪"，任务卡推进至卡1（CMake） | commit 123286a 成功推送到 origin/main |
| 2026-08-07 | 新增AGENTS.md跨Agent协作协议；Skill迁入工程 .qoder/skills/；本仓库成为多主机Agent进度共享总线 | 用户明确要求跨主机Agent经本仓库同步进度 |
| 2026-08-09 | 对照V1.2原始规范优化全部非src协议层：补公开README、统一状态/Git规则、强化隐私与模板边界；未改变冻结方案或Gate验收 | 原始规范19页渲染与结构化审计；本次文档一致性、链接与隐私检查 |
| 2026-08-09 | C++主线不变；新增AI-0—AI-3渐进自治、Gate B Agent证据包与最小生产闭环、Gate C最小部署恢复；Redis/Kafka/K8s停放 | 用户授权；官方Agent交付流程、学习研究与当前岗位样本交叉核对；DECISION_LOG记录 |
| 2026-08-10 | 跨主机同步保持最小机制：启动时拉取远端并读取Skill/状态；稳定用户情况变化时更新Skill并立即走提交、复核、推送流程 | 用户明确要求只保证最新情况上GitHub且避免复杂检测机制 |
| 2026-08-10 | 卡1首版时间盒完成并进入AI-1 Review；`src/`内单文件工程配置、构建、运行成功，但根目录配置失败且缺少独立头/实现文件，Gate A验收不勾选 | 实际命令输出与工作树现场；`cmake -S . -B build`报告根目录无`CMakeLists.txt` |
| 2026-08-10 | 卡1经AI-1 Review完成并推进至卡2；用户补齐根目录CMake、独立头文件和实现文件，并修正most vexing parse与成员函数返回类型错误 | 全新临时目录配置、编译、链接和运行成功；clangd检查两个源文件均为0错误 |
| 2026-08-10 | 确认新版AGENTS启动/结束协议已在仓库生效；补充嵌套CMake构建目录、Makefile和本地可执行文件的忽略规则，保留现场但不纳入提交 | 工作树仅含本地生成物；未改变Gate A验收结果 |
