# PROJECT_STATE — 单一事实源

> 本文件是当前进度的唯一权威记录。Agent在新主机或新任务批次开始时先读本文件；批次内有新事实就本地更新，用户明确收尾时统一同步。
> 用户不需要记住自己进行到哪一步；以本文件为准。

## 当前状态（2026-08-22 更新）

- **当前模式**：学习
- **当前交付门**：A（C++准入包）
- **当前任务卡**：卡3（RAII文件描述符）
- **当前任务AI自治等级**：AI-0（实验B移动构造首版）
- **已通过的验收**：本地仓库 + 远程备份就绪；15分钟建立多文件CMake工程；90秒讲清编译链接；卡3实验A作用域析构
- **当前唯一任务**：阅读 [Gate A卡3](tasks/gate-a-card-03-raii-fd.md) 的“实验B/C前再读”，只做实验B移动构造：让新对象接管fd、源对象进入`-1`无效状态，并用系统调用证据确认两个对象析构后资源只关闭一次；先不实现移动赋值和实验C—D，不要求额外填写固定回复模板
- **最近一次构建/测试命令与结果**：2026-08-22，实验A第五版将有效判断修正为`fd >= 0`；根目录重新配置和全量构建成功，普通运行、关闭标准输入后复用fd 0的定向运行、ASan/UBSan运行均退出码为0；两条运行路径中接管端析构后均为`EBADF`，未接管端`F_GETFD`均返回`0`。结合用户连续Diff已观察到模型从“对象拥有管道两端”修正为“对象唯一拥有一个fd、未接管端由测试代码负责”，实验A通过
- **最近环境/文档验证**：2026-08-11，完成训练质量V2设计与一手研究交叉核对：保持C++/Gate内容不变，新增七维长期能力模型、自适应支架渐退、错误类型诊断、三行自解释、24—72小时陌生迁移和AI收益/风险校准；延迟任务嵌入后续训练，不增加每周总时长

## 下一动作

只执行 [Gate A卡3](tasks/gate-a-card-03-raii-fd.md) 实验B：读对应最小理论，主写移动构造并让源对象失效，再用系统调用观察资源只关闭一次；不提前实现移动赋值或实验C—D。

完成标准：

- 实验A—C分别留下运行前预测和系统调用证据，能够解释作用域析构、移动构造和移动赋值后的fd状态。
- 最终实现保持一个文件描述符只有一个所有者，无效值和所有权转移规则清晰；析构与移动不会泄漏或重复关闭，拷贝被禁止。
- 实验D能主动制造并捕获一次所有权错误，随后恢复正确实现并通过回归。
- 24—72小时后能在陌生缺陷Diff中识别一个fd所有权不变量破坏，并设计系统调用验证；该检查限时10—15分钟并嵌入后续训练。

## 阶段进度看板

### Gate A：C++准入包（进行中）

- ☑ 本地仓库 + 远程备份就绪（2026-08-07，commit 123286a 推送成功）
- ☑ 15分钟建立多文件CMake工程（2026-08-10，根目录配置、构建、运行及clangd检查通过）
- ☑ 90秒讲清编译链接（2026-08-10，多轮闭卷回答与定向追问后覆盖阶段、错误类型和当前工程映射）
- ☐ RAII文件描述符闭卷实现
- ☐ BoundedQueue支持容量、等待、关闭和join
- ☐ PacketCodec处理半包、多包、非法长度
- ☐ 内存版装备强化失败时状态不变
- ☐ TSan发现并修复一次数据竞争
- ☐ 安排校准面试（Gate A全部勾选后才进入）

### Gate B：端到端求职启动包（未开始）

- ☐ 测试客户端完成连接、查询和强化
- ☐ Session重复关闭安全并最终析构
- ☐ SendQueue保持有序且有上限
- ☐ 命令线程与网络线程边界清晰
- ☐ 请求中断线后晚到响应安全丢弃
- ☐ ASan无已知问题
- ☐ 用户能画线程/所有权图并讲5分钟链路
- ☐ Linux下build/test/run/stop可重复，日志支持一次故障定位
- ☐ 本地检查脚本或CI至少有一个自动质量门
- ☐ 一次AI-2证据包通过：任务简报、完整Diff Review、拒绝/修正、硬验证、贡献说明
- ☐ 开始主要投递

### Gate C：反馈强化（未开始，默认MySQL事务 + 最小Docker/Compose + AI-3证据）

- ☐ request_id唯一约束生效
- ☐ 同一请求重复提交只产生一次结果
- ☐ 不同请求并发改同一装备保持正确
- ☐ 金币不足不会变负
- ☐ 任意中间失败全部回滚
- ☐ 装备变化流水可用于审计
- ☐ 数据库错误不会令进程崩溃
- ☐ Docker/Compose可以启动、停止并完成一次恢复演练
- ☐ 一次AI-3端到端证据通过，用户能说明风险、验证、发布和回滚

## 升级判定规则（Agent必须主动检查）

- 仅当上方某个Gate的全部验收项勾选后，才允许宣布进入下一阶段。
- 每次涉及进度的回复中，Agent必须对照看板说明：本次推进了哪些项、还差哪些项、是否达到升级条件。
- Gate A完成 → 提醒安排校准面试，阻止"再准备一点"。
- Gate B完成 → 提醒开始主要投递，不把MySQL当前置门槛。

## 变更历史

| 日期 | 变化 | 依据 |
|---|---|---|
| 2026-08-07 | 初始化状态文件；项目处于Gate A起点，任务卡0—2 | 仓库仅有初始提交，无C++工程 |
| 2026-08-07 | 勾选"仓库+远程备份就绪"，任务卡推进至卡1（CMake） | commit 123286a 成功推送到 origin/main |
| 2026-08-07 | 新增AGENTS.md跨Agent协作协议；Skill迁入工程 .qoder/skills/；本仓库成为多主机Agent进度共享总线 | 用户明确要求跨主机Agent经本仓库同步进度 |
| 2026-08-09 | 对照V1.2原始规范优化全部非src协议层：补公开README、统一状态/Git规则、强化隐私与模板边界；未改变冻结方案或Gate验收 | 原始规范19页渲染与结构化审计；本次文档一致性、链接与隐私检查 |
| 2026-08-09 | C++主线不变；新增AI-0—AI-3渐进自治、Gate B Agent证据包与最小生产闭环、Gate C最小部署恢复；Redis/Kafka/K8s停放 | 用户授权；官方Agent交付流程、学习研究与当前岗位样本交叉核对；DECISION_LOG记录 |
| 2026-08-10 | 跨主机同步保持最小机制：启动时拉取远端并读取Skill/状态；稳定用户情况变化时更新Skill并立即走提交、复核、推送流程 | 用户明确要求只保证最新情况上GitHub且避免复杂检测机制 |
| 2026-08-10 | 卡1首版时间盒完成并进入AI-1 Review；`src/`内单文件工程配置、构建、运行成功，但根目录配置失败且缺少独立头/实现文件，Gate A验收不勾选 | 实际命令输出与工作树现场；`cmake -S . -B build`报告根目录无`CMakeLists.txt` |
| 2026-08-10 | 卡1经AI-1 Review完成并推进至卡2；用户补齐根目录CMake、独立头文件和实现文件，并修正most vexing parse与成员函数返回类型错误 | 全新临时目录配置、编译、链接和运行成功；clangd检查两个源文件均为0错误 |
| 2026-08-10 | 确认新版AGENTS启动/结束协议已在仓库生效；补充嵌套CMake构建目录、Makefile和本地可执行文件的忽略规则，保留现场但不纳入提交 | 工作树仅含本地生成物；未改变Gate A验收结果 |
| 2026-08-10 | 卡2首次90秒闭卷口述完成但未通过；三类错误阶段判断正确，术语、编译单元边界和当前工程映射不完整 | 用户原始口述；Agent按卡2完成标准逐项Review |
| 2026-08-10 | 卡2第二次闭卷口述仍未通过；已修正术语并掌握四阶段作用，但把头文件参与预处理与目标文件参与链接混为一谈 | 用户第二次原始口述；Agent将剩余缺口收敛为三问微验收 |
| 2026-08-10 | 卡2通过并推进至卡3 RAII文件描述符；用户经闭卷纠正后能解释编译单元、目标文件、链接输入输出和唯一程序入口 | 用户连续闭卷回答；最后能解释仅`t1.o`缺少定义`main()`的`main.o`，无法形成当前可执行目标 |
| 2026-08-11 | 卡3第一次设计回答作废，不计能力失败；原题使用开放提问却按未声明的Linux fd窄前提评分，现已固定对象、平台、所有权模型与验证API并改写为七个窄问题 | 用户指出题面宽度与标准答案不一致；Agent复核后确认出题错误并撤销原判定 |
| 2026-08-11 | 卡3从七个标准化理论问题改为四个递增实作实验；训练默认以真实场景、预测、实现、系统调用证据和偏差解释形成理解 | 用户明确反对机械应试和填鸭式教育；Agent确认此前将验收清单误当学习方法 |
| 2026-08-11 | 将长期北极星明确为全球一线软件工程师能力水平；近期C++生产准入路线不变，提高问题定义、系统建模、工程品味、生产所有权和AI原生交付的训练标准 | 用户明确长期目标为“硅谷精英”；Agent将其转化为可训练能力而非公司或地域标签 |
| 2026-08-11 | 任务卡新增分阶段最小理论包；默认由Agent提供必需知识、API契约、易错点和官方入口，资料检索只作为限时刻意训练 | 用户提出卡内理论可节省搜索、翻译和机翻解读成本，同时要求权衡检索能力收益 |
| 2026-08-11 | 将检索训练收敛为高价值证据获取，并把广度预算和停止条件设为每张卡必备项 | 用户追问AI时代检索能力的重要性并补充广度控制；Agent判断信息筛选与停止比手工搜索更关键 |
| 2026-08-11 | 跨主机同步改为任务批次检查点：新主机/新任务启动时同步一次，连续对话内本地记录且不反复拉取，用户明确提交/同步/收尾时统一审查、提交、拉取和推送 | 用户指出逐消息远程拉取延迟高，并明确本批次结束时再同步；该规则替代2026-08-10的“稳定变化立即推送”时机 |
| 2026-08-11 | 训练质量升级为V2：技术内容和Gate不扩张；新增七维长期能力、自适应支架与撤除、六类障碍诊断、单点高杠杆反馈、三行自解释、延迟陌生迁移和AI判断校准 | 用户要求短期求职目标不变，训练方法多轮迭代到符合AI时代世界级工程师标准；编程学习与AI编码一手研究交叉核对，DECISION_LOG记录适用边界 |
| 2026-08-22 | 卡3实验A首版完成构建和系统调用观察，但暂未通过；当前类型同时接管`pipe()`两端，默认构造还留下未初始化fd，未形成“单个fd唯一所有者”不变量 | 根目录重新配置、编译、链接和运行成功；析构后对两个保存fd执行`fcntl(F_GETFD)`均得到`EBADF` |
| 2026-08-22 | 卡3实验A第二版已修正管道两端的所有权边界，但为传递单个fd使用`new int[1]`，引入无人释放的额外堆资源，实验仍未通过 | 正常构建运行显示接管端为`EBADF`、未接管端仍有效；LeakSanitizer报告`main.cpp`分配的4字节直接泄漏 |
| 2026-08-22 | 卡3实验A第三版已去除额外堆资源，正常行为和ASan/UBSan均通过；但默认构造仍留下未初始化fd且析构无条件关闭，无效状态不变量尚未成立 | 根目录重新配置、全量构建和运行成功；Sanitizer运行退出码为0且无泄漏；源码Review确认`t1::t1(){}`未初始化成员 |
| 2026-08-22 | 卡3实验A第四版已建立默认`-1`状态和条件关闭，但把有效条件写成`fd > 0`，遗漏Linux合法fd 0，实验仍未通过 | 关闭标准输入后运行使`pipe()`复用fd 0；析构后`fcntl(0, F_GETFD)`仍返回`0`，直接证明fd 0未关闭 |
| 2026-08-22 | 卡3实验A第五版的代码和硬验证通过；普通fd与fd 0边界都能在析构后关闭接管端，未接管端保持有效，Sanitizer无报告；待三行自解释完成学习闭环 | 根目录全量构建成功；普通运行、关闭标准输入后的定向运行、ASan/UBSan运行均退出码为0；两条行为路径均观察到接管端`EBADF`、未接管端有效 |
| 2026-08-22 | 卡3实验A通过并推进至实验B；连续实现修正和硬证据已充分显示所有权模型变化，不再要求用户为满足格式补交三行自解释 | 用户从同时接管管道两端，依次修正为单fd所有者、无额外堆资源、默认`-1`和覆盖fd 0；普通路径、fd 0定向路径及ASan/UBSan均通过 |
