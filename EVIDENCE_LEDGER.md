# EVIDENCE_LEDGER — 能力证据台账

> 记录可验证、可面试的能力证据。只记真实证据，不夸大个人项目、商业C++经验、性能数据或AI贡献。
> 诚实区分：个人项目 / 商业经验，以及用户主写 / Agent生成 / 用户改写与验证。Agent产出只有在用户Review、解释和硬验证后才能成为“Agent协作交付”证据，不能冒充独立手写能力。

| 日期 | 证据类型 | AI等级与贡献边界 | 内容 | 验证方式 |
|---|---|---|---|---|
| — | — | — | 尚无记录 | — |
| 2026-08-10 | 构建/测试（未通过卡1） | AI-0首版由用户完成；Agent仅做AI-1 Review与硬验证 | 在`src/`内的单文件`main.cpp`配合`src/CMakeLists.txt`可配置、构建并运行，输出`Hello World`；但仓库根目录无`CMakeLists.txt`，且没有独立头文件与独立实现文件 | `cmake -S src -B src/build`、`cmake --build src/build`及运行成功；`cmake -S . -B build`失败；读取工作树文件与`git status` |
| 2026-08-10 | 构建/测试（卡1通过） | 多文件工程及修正均由用户主写；Agent在AI-1下Review、定位most vexing parse和返回类型错误并执行硬验证 | 根目录`CMakeLists.txt`将`main.cpp`与`t1.cpp`组成`CppCommandServer`，共享声明位于独立头文件；配置、编译、链接和运行成功 | 全新临时构建目录执行根目录配置与详细构建；两个对象文件成功链接；运行输出两行预期结果；clangd基于编译数据库检查两个源文件均为0错误 |
| 2026-08-10 | 闭卷口述（卡2未通过） | AI-0由用户独立口述；Agent仅按标准Review | 能正确判断头文件缺失发生在预处理、类型错误发生在编译、声明存在但定义缺失通常在链接阶段失败；尚未准确区分“预处理/预编译”、单编译单元与最终链接，也未覆盖当前工程文件映射 | 对照卡2口述完成标准审查用户原始回答 |
| 2026-08-10 | 闭卷口述第二次（卡2未通过） | AI-0由用户独立口述；Agent仅按标准Review | 已能准确描述预处理、编译、汇编和链接的主要作用，并说出最终目标`CppCommandServer`；仍误认为`t1.cpp`与`t1.hpp`在链接阶段组合生成目标文件 | 对照当前工程的编译单元、目标文件和链接目标审查用户第二次回答 |
| 2026-08-10 | 闭卷口述（卡2通过） | 用户在AI-0下连续回答；Agent只做纠错和定向追问 | 用户最终能说明头文件在预处理阶段进入编译单元、编译/汇编生成目标文件、链接器以目标文件为输入生成`CppCommandServer`，并解释当前链接必须包含定义唯一`main()`的`main.o` | 对照当前`main.cpp`、`t1.cpp`、`t1.hpp`及链接目标逐项追问；如实保留前两次未通过记录 |
| 2026-08-22 | RAII实验A首版（未通过） | 用户在AI-0下主写首版；Agent进入AI-1读取Diff并重新构建运行 | 首版能通过析构和`fcntl(F_GETFD)`观察到fd关闭，但类型同时接管`pipe()`两端，默认构造留下未初始化fd；尚未建立“一个对象只拥有一个fd，另一端仍由测试代码拥有”的不变量 | `cmake -S . -B build`、`cmake --build build --clean-first`和运行均成功；析构后两个保存fd均返回`EBADF`，与卡片要求的单fd所有权模型不符 |
| 2026-08-22 | RAII实验A第二版（未通过） | 用户在AI-1 Review后主写所有权修正；Agent重新构建并增加ASan验证 | 第二版已让对象只关闭接管端，未接管端由测试代码保持和关闭；但调用处使用`new int[1]`传递fd且无人释放，形成新的堆资源所有权缺口 | 正常运行中接管端返回`EBADF`、未接管端`F_GETFD`返回`0`；`g++ -std=c++17 -fsanitize=address`运行后LeakSanitizer报告4字节直接泄漏，定位到`main.cpp`的数组分配 |
| 2026-08-22 | RAII实验A第三版（未通过） | 用户继续主写最小修正；Agent执行正常构建、行为检查和ASan/UBSan验证 | 已改为按值接收和保存单个fd，删除额外堆分配；有效路径行为正确且Sanitizer无报告，但默认构造的fd成员未初始化，析构无条件读取并关闭，尚未形成明确无效状态 | 根目录配置、全量构建和运行成功；接管端返回`EBADF`、未接管端`F_GETFD`返回`0`；`-fsanitize=address,undefined`运行退出码为0；源码Review定位未覆盖的默认构造路径 |
| 2026-08-22 | RAII实验A第四版（未通过） | 用户继续主写无效状态修正；Agent设计并执行fd 0定向边界实验 | 默认状态已为`-1`，普通路径和Sanitizer均通过；但析构使用`fd > 0`，遗漏合法fd 0，定向实验可稳定观察到资源未关闭 | 先关闭标准输入再运行程序，使`pipe()`读端获得fd 0；析构后`fcntl(0, F_GETFD)`返回`0`而非`EBADF`，证明边界判断错误 |
| 2026-08-22 | RAII实验A通过 | 用户主写全部实验代码并完成历次修正；Agent在AI-1下Review、设计fd 0反例并执行构建与Sanitizer验证 | 用户的连续Diff已显示模型从“对象接管管道两端”修正为“对象唯一拥有一个fd，未接管端由测试代码负责”；单fd所有权、默认`-1`、覆盖fd 0的有效判断和测试端清理边界均成立，无需为格式额外复述 | 根目录全量构建成功；普通路径与fd 0定向路径均观察到接管端`EBADF`、未接管端有效；ASan/UBSan运行退出码为0且无报告 |
| 2026-08-22 | RAII实验B通过 | 移动构造与实验代码由用户在AI-0下主写；Agent进入AI-1 Review并设计独立状态/系统调用探针 | 移动构造把fd交给目标对象并把源对象设为`-1`；目标析构后接管fd无效，测试代码持有的管道另一端保持有效 | 全新根目录配置、构建及ASan/UBSan运行退出码为0；独立探针输出`B_STATE=1 B_CLOSE=1 B_OTHER_VALID=1` |
| 2026-08-22 | RAII实验C首版（未通过） | 移动赋值与实验代码由用户主写；Agent在AI-1下Review并执行双资源最小反例 | 首版能把源fd交给目标并令源对象失效，但赋值前没有释放目标原有fd；当前`main`的目标是已移动后的空对象，因而正常运行与Sanitizer没有触发泄漏路径 | 双资源探针让源对象和目标对象各自拥有不同fd；输出`C_STATE=1 C_OLD_CLOSED=0 C_INCOMING_CLOSED=1`并退出1，`fcntl(F_GETFD)`证明目标旧fd在赋值后仍有效 |
| 2026-08-22 | RAII实验C补充版（未通过） | 用户根据Review主写目标旧fd释放和双资源场景；Agent重建、运行Sanitizer并增加重复关闭探针 | 正常移动赋值已能释放目标旧fd并完成唯一所有权转移；但释放动作在自移动判断之前，自移动会先关闭自身fd；测试代码在所有者析构后还重复关闭已移交的`p2` | 根目录构建、运行和ASan/UBSan退出0；双资源探针输出`C_STATE=1 C_OLD_CLOSED=1 C_INCOMING_CLOSED=1`并退出0；重复关闭探针输出`SELF_FD_VALID_AFTER_ASSIGN=0`、`CALLER_REPEAT_CLOSE_RESULT=-1 ERRNO=9`并退出1 |
| 2026-08-22 | RAII实验C再次修正（未通过） | 用户把资源释放与转移放入非自移动分支；Agent重建并分别验证双资源、自移动和当前程序实际关闭次数 | 移动赋值实现本体已能安全处理正常与自移动路径；测试程序仍在RAII析构后调用`close(p2)`，形成一次可观察的重复关闭 | 根目录构建与ASan/UBSan退出0；双资源探针输出`C_STATE=1 C_OLD_CLOSED=1 C_INCOMING_CLOSED=1`，自移动探针输出`SELF_VALID_AFTER_ASSIGN=1 SELF_CLOSED_AFTER_SCOPE=1`；链接器包装探针记录fd 4第二次关闭为`REPEATED=1 RESULT=-1 ERRNO=9` |
| 2026-08-22 | RAII实验C通过 | 移动赋值、双资源场景及历次修正均由用户主写；Agent在AI-1下设计正常、自移动和关闭次数探针并执行最终复验 | 用户已修复目标旧fd泄漏、自移动提前关闭和测试端重复关闭；正常移动时旧资源先释放、新资源唯一转入，源对象失效，自移动保持原资源，调用方不再关闭已移交fd | 根目录全新构建、普通运行及ASan/UBSan退出0；双资源探针输出`C_STATE=1 C_OLD_CLOSED=1 C_INCOMING_CLOSED=1`，自移动探针输出`SELF_VALID_AFTER_ASSIGN=1 SELF_CLOSED_AFTER_SCOPE=1`；关闭包装记录fd 3、fd 4各一次且无重复 |
| 2026-08-22 | RAII实验D根因定位（进行中） | Agent按用户批准的边界注入一个陌生缺陷；用户在AI-0下独立Review，Agent仅确认诊断与注入点一致 | 用户正确识别移动构造只把fd值交给新对象、未令源对象失效，违反“一个有效fd最多一个关闭责任”的不变量，并预测两个析构路径会重复关闭 | 当前源码Review与用户原始诊断一致；fd数字复用和误关新资源的系统调用证据、修复及A—C回归尚未完成，因此实验D未通过 |
| 2026-08-22 | RAII实验D通过 | Agent注入并在诊断后恢复一个陌生缺陷；用户在AI-0下独立Review和解释根因；Agent仅以仓库外临时探针做RED—GREEN与回归 | 用户识别出移动构造未令源对象失效会产生两个关闭责任，并能推演fd数字复用后过期对象误关新资源的生产风险；用户基于边际收益主动停止扩张主程序，未把未运行的复用场景冒充实测证据 | 缺陷版探针输出`SOURCE_INVALID=0 TARGET_OWNS=1 CLOSED_AFTER_SCOPE=1`且第二次`close`为`EBADF`；恢复后输出`SOURCE_INVALID=1 TARGET_OWNS=1 CLOSED_AFTER_SCOPE=1`；普通版、ASan/UBSan版及覆盖A/B/C、自移动、fd 0的综合探针全部退出0 |
| 2026-08-22 | RAII析构异常解释（进行中） | 用户在AI-0下独立回答；Agent仅校准语言边界 | 用户能说明处理其他异常并进行栈展开时，如果析构函数再让异常逃逸，会触发`std::terminate`并终止程序；尚待说明当前首版如何处理`close()`结果 | 对照用户原始回答与当前`t1::~t1()`实现检查；未把尚未回答的错误处理策略计为通过 |
| 2026-08-24 | RAII析构错误处理解释通过 | 用户在AI-0下分两次独立回答；Agent仅校准默认析构不允许异常逃逸的边界 | 用户能说明栈展开期间析构再次抛异常可能触发`std::terminate`，并能结合当前实现说明`close()`失败时记录错误输出而不是抛出异常 | 对照用户两次原始回答及`t1::~t1()`中的返回值检查、`errno`错误输出路径完成验收 |
| 2026-08-24 | RAII延迟迁移首答（未通过） | 用户在AI-0下独立Review陌生`duplicate()`缺陷；Agent仅按已声明契约与证据标准复核 | 用户识别出直接复制fd数字会形成两个关闭责任，并预测析构顺序导致一次成功和一次失败；但建议令原对象失效，实际把“独立复制”改成了“移动”，且验证仅依赖输出文字 | 对照题面“原对象与返回对象可独立析构”的契约；尚缺两个独立fd的判断，以及销毁一个对象后用`fcntl(F_GETFD)`检查另一个fd仍有效的系统调用证据设计 |
| 2026-08-24 | RAII延迟迁移提示后修正（不计独立通过） | Agent提供`fcntl(F_GETFD)`验证点后，用户独立判断预期结果 | 用户正确判断副本析构后，原fd在当前正确场景下应返回`0`，错误实现返回`-1`；通用判断应为有效时返回非负标志、无效时返回`-1`且`errno`为`EBADF` | 对照`fcntl(F_GETFD)`契约校准；由于关键验证方法来自Agent提示，本条只记录支架后修正，不满足无Agent陌生迁移验收 |

| 2026-09-21 | 名称整理与网络源码审查（非能力验收） | 用户授权重命名；Agent完成机械修改、AI-1 Review 和临时验证，未实现核心网络功能 | example/Session 改为 NetworkCore/TcpConnection，演示目标为 NetworkCoreDemo；仍仅构建入口，不能据此认为网络层通过 | 更名前后演示构建/运行通过；脚本、JSON、软链接和调试路径检查通过；定向检查复现构造缺参、玩家查询缺少定义、FC CD 被读为 00 CD、有效长度变为 -1、头文件函数重复定义；完整发现见 NetworkCore/README.md，未做 TCP 或多线程验收 |

| 2026-09-22 | 网络层接入准备度 Review（未通过） | 用户主写连接读写与管理器；Agent仅审查和执行临时探针，未修复功能 | 读写与发送队列骨架形成；真实启动、协议拆帧和连接释放仍有缺口，不能判定只差 Protobuf | 新目录演示构建/运行通过，编译数据库只含 main；手动网络源码编译链接通过；管理器析构链接缺失；FCCD/12348001 被读成 FFCD/FFFF8001，UBSan 负值左移失败；详见 NetworkCore/reviews/2026-09-22-readiness.md，未做 TCP/并发验收 |

| 2026-09-22 | Python TCP 测试夹具交付（非能力验收） | 用户授权；Agent实现独立客户端和自测，C++ 核心源码未修改 | 提供默认 9981 端口的交互/单次 raw 与 18 字节帧协议收发、分段/批量发送及错误观察工具，便于用户复现服务端问题 | 原始字节往返测试先失败；`python3 -B NetworkCore/tools/test_tcp_client.py -v` 使用真实本地 TCP 对端 9/9 通过；未与 C++ 服务端联调，不代表网络层或 Gate 通过 |

证据类型参考：构建/测试输出、闭卷复现、口述链路、Bug排障记录、所有权/线程图、git渐进提交、AI任务简报与Diff决策、故障演练、校准/真实面试表现。

AI-2/3证据至少包含：任务简报、完整Diff Review、接受/拒绝/改写的关键决策、硬验证结果、用户能解释的贡献边界。不要保存冗长Prompt或聊天转录。
