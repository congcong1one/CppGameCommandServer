# 模块设计卡（半页）

> 学习模式下，核心模块实现前由用户填写；Agent不得代写第一版。

## 模块名称

（例如：BoundedQueue）

## 问题

这个模块解决什么问题？不做它会发生什么？

## 输入 / 输出

- 输入：
- 输出：
- 调用方是谁，在哪个线程调用？

## 所有权

- 谁创建这个对象？谁销毁？
- 内部持有哪些资源（buffer、socket、线程、连接）？
- 若涉及shared_ptr：控制块、循环引用、析构时点如何处理？

## 线程

- 哪些线程会访问它？
- 哪些数据需要保护？用什么锁？持锁期间绝不做什么？
- 启动和停止（join）的顺序是什么？

## 失败路径

- 至少列出两个失败场景（例如：队列已关闭再push、容量满、对端断线）。
- 每个失败场景的行为是什么（阻塞/返回错误/丢弃）？

## 测试

- 正常路径测试：
- 边界路径测试：
- 失败路径测试：
- 是否需要Sanitizer（TSan/ASan/UBSan）验证？
